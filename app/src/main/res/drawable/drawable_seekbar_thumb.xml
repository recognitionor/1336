<?xml version="1.0" encoding="utf-8"?>
<!-- 원모양의 시크바 컨트롤러 -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false"  >
    <!-- 배경 -->
    <solid
        android:color="@color/white"/>
    <!-- 테두리 -->
    <stroke
        android:width="8dp"
        android:color="@color/sub_color_gradient_red" />
    <!-- 크기 -->
    <size
        android:width="24dp"
        android:height="24dp"/>
</shape>

